# === NUNCA subir: secretos ===
.env

# === Entorno virtual (se reinstala con requirements.txt) ===
venv/
__pycache__/
*.pyc

# === Logs locales ===
logs/

# === Base de datos re-descargable (la baja el pipeline cada vez) ===
results.csv

# === Archivos de prueba (forzarían datos falsos en el pipeline si se suben) ===
wc_results_mock.json
wc_ko_mock.json

# === Digest/resumen generado (se regenera cada día/semana) ===
digest.md
digest.html
weekly.md
weekly.html

# === Guía completa pre-Mundial (se regenera) ===
preview.md
preview.html

# === Dato curioso del día (días sin partido; se regenera) ===
curio_today.json

# === Cierre del día (resultados vs pronóstico; se regenera) ===
recap.png
recap.mp4
recap_voice.mp3
recap_voiceover.txt
recap_caption.txt
recap_digest.md
recap_digest.html
recap_today.json

# === Artefactos del reel/voz (locales, por día) ===
voiceover.txt
caption.txt
youtube.txt
recap_youtube.txt
hook.txt
voice.mp3
matchday.mp4
matchday.png
_frame*.png

# === Banderas cacheadas (se re-descargan de flagcdn) ===
flags/

# === Banner y posts de contenido (locales) ===
banner.png
intro_post.html
intro_post.md

# === Archivos temporales del SO ===
Thumbs.db
.DS_Store

# === Post tecnico (se regenera de POST_TECNICO.md) ===
tech_post.html

# afinador de voz (artefactos locales)
cand_*.mp3
cand_*.srt
ref_voz.mp3
voice_v3.mp3

# === Videos VIRALES (Remotion): salidas generadas por día (van como artefactos, no al repo) ===
pronostico_viral.mp4
recap_viral.mp4
voice_words.json
recap_voice_words.json
viral_pronostico.json
viral_recap.json
voice_avatar.webm
recap_voice_avatar.webm

# === Remotion: dependencias y artefactos (node_modules NUNCA al repo; el resto se regenera) ===
video_remotion/node_modules/
video_remotion/out/
video_remotion/public/
video_remotion/.remotion/

# Entorno virtual local (Claude Code)
.venv/

# Calibrador experimental (regenerable con backtest.py; off por defecto)
calibration.json

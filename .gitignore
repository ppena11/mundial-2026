# === NUNCA subir: secretos ===
.env

# === Entorno virtual (se reinstala con requirements.txt) ===
venv/
__pycache__/
*.pyc

# === Logs locales ===
logs/

# === Base de datos re-descargable (la baja el pipeline cada vez) ===
results.csv

# === Archivos de prueba (forzarían datos falsos en el pipeline si se suben) ===
wc_results_mock.json
wc_ko_mock.json

# === Digest/resumen generado (se regenera cada día/semana) ===
digest.md
digest.html
weekly.md
weekly.html

# === Dato curioso del día (días sin partido; se regenera) ===
curio_today.json

# === Artefactos del reel/voz (locales, por día) ===
voiceover.txt
caption.txt
hook.txt
voice.mp3
matchday.mp4
matchday.png
_frame*.png

# === Banderas cacheadas (se re-descargan de flagcdn) ===
flags/

# === Banner y posts de contenido (locales) ===
banner.png
intro_post.html
intro_post.md

# === Archivos temporales del SO ===
Thumbs.db
.DS_Store
